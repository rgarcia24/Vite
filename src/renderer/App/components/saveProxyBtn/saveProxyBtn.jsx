import React from 'react'

import './saveProxy.css'
const saveProxyBtn = ({ saveProxy }) => {
    return (

        <button className='saveProxy-btn' onClick={saveProxy}>Save</button>

    )
}

export default saveProxyBtn
